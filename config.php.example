<?php

// MySQL configuration
$MYSQL_HOST = "localhost";
$MYSQL_USER = "root";
$MYSQL_PASSWORD = "";
$MYSQL_DB = "";

// Default set ID to display on dashboard.
$DEFAULT_SET_ID = 1;

// List of aggregation values to show on each sensor in the dashboard.
$DASH_VALUES = array(
	"last" => "Aktuální",
	"dayMax" => "Max. za 24h",
	"dayMin" => "Min. za 24h"
);

// Default sensor ID to display in kiosk.
$DEFAULT_SENSOR_ID = 1;

// Default period of chart history. In hours.
$DEFAULT_HISTORY_PERIOD = 24;
