<?php

// MySQL configuration
$MYSQL_HOST = "localhost";
$MYSQL_USER = "root";
$MYSQL_PASSWORD = "";
$MYSQL_DB = "";

// Default set ID to display on dashboard.
$DEFAULT_SET_ID = 1;

// List of aggregation values to show on each sensor in the dashboard.
$DASH_VALUES = array(
	"last" => "Aktuální",
	"dayMax" => "Max. za 24h",
	"dayMin" => "Min. za 24h"
);

// Default sensor ID to display in kiosk.
$DEFAULT_SENSOR_ID = 1;

// Default period of chart history. In hours.
$DEFAULT_HISTORY_PERIOD = 24;

// API configuration
$API_AUTH_TOKEN = "";  // Authentication token for API
$API_URL = "";  // URL of API
$API_CACHE = "/tmp/readings.db3";  // Cache where to store readings that failed to be sent to server.

